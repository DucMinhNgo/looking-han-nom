# Dev/test deps.
#
#   python -m venv .venv && .venv/bin/pip install -r requirements.txt -r requirements-dev.txt
#   .venv/bin/python -m pytest
pytest==9.1.1
pytest-asyncio==1.3.0
pytest-cov==7.0.0

# fastapi's TestClient is built on httpx, which the runtime itself no longer needs.
httpx==0.28.1

# Only for generating the sample images in scripts/make_sample.py.
Pillow==10.4.0
