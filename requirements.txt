# Lookup tool runtime deps. Reads a mounted dataset and image folder, and lets
# an admin add to both — so there is no model, no object store, and no HTTP
# client in the runtime.

# Web layer
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Multipart form parsing — every upload in routes_data.py needs it. FastAPI
# raises at import time without it, so the app will not start.
python-multipart==0.0.9

# Auth
bcrypt==4.2.0
PyJWT==2.9.0

# Optional Qwen-compatible OpenAI client. The app still starts without a key.
openai==1.51.0
# openai 1.51 still passes the deprecated `proxies` argument to httpx.
httpx==0.27.2
# Optional Redis cache for expensive Qwen analysis.
redis==5.2.0
